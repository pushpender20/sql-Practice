# SELECT

SELECT is used to retrieve data from a table.

Syntax:

SELECT column_name
FROM table_name;

Example:

SELECT first_name
FROM employees;


# WHERE

Used to filter rows.

Operators

=
<>
>
<
>=
<=
LIKE
BETWEEN
IN